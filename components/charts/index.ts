// PR3-C2 — the chart primitives, extracted from the reports cards so
// any surface (owner home, fees, ops) can render the same inline SVG
// without importing from a feature folder.
export {
  ColumnChart,
  Donut,
  HorizontalBars,
  LineTrend,
  type DonutSlice,
} from "./analytics-charts";
