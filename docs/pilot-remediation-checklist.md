# Pilot remediation register

**Audited commit:** `c198627fa0e41f6268b75d3a8c2dd2f18d6a3d51` (merged PR #191). Source: `docs/audits/2026-09-23-post-pr191-comprehensive-audit.md` (SHA-256 before and after branch switch: `f88d8d64c08f9fe9195d5be85f64f39699fb2e3250bb5fbe24158d2e0fd01e8e`).

**Workflow:** protected `main`, short-lived `fix/pilot-safety` for PR-A, human merge only. PR-B starts only after PR-A is merged by the human. A checkbox is complete only after acceptance and tests pass; evidence includes the exact command/results, migration if any, commit SHA and deviations. This register does not grant release approval.

| Field | Current value |
|---|---|
| Current status | PR-A opened as [#192](https://github.com/saurabh22suman/aqua/pull/192). PR `ci` passed (run `35993671881`, 18m34s); `agent-protected-paths` failed without the human-only `human-approved-merge` label. No new migrations. |
| Current task | Agent work on PR-A complete; awaiting human review and protected-path label. |
| Next task | After the human merges PR-A, begin PR-B separately. Do not self-merge or change release gates. |
| Known blockers | Human protected-path label/review pending; PR-B offsite backup/restore, deployment parity and operational pilot checks remain open. No pilot release approval. |

## PR-A — Pilot Safety

- [x] **A0: Preserve the source-first audit and this task register.** Acceptance: report checksum unchanged and both files committed on `fix/pilot-safety`; no existing work overwritten. Evidence: `git status --short` showed only the audit before switching; `git ls-tree -r --name-only origin/main -- docs/audits/2026-09-23-post-pr191-comprehensive-audit.md docs/pilot-remediation-checklist.md` returned neither path; `sha256sum docs/audits/2026-09-23-post-pr191-comprehensive-audit.md` = `f88d8d64c08f9fe9195d5be85f64f39699fb2e3250bb5fbe24158d2e0fd01e8e` before and after switch and after commit `5251dd3`. Migration: none. Deviation: none.
- [x] **A1 / F01: Patched Next.** Acceptance: smallest compatible patched Next resolving GHSA-2xp9-vwfh-vxw4, lockfile updated normally; `_next/image` reachability assessed; remaining `pnpm audit --prod` advisories classified individually. Evidence: `pnpm up next@15.5.24 eslint-config-next@15.5.24`, then exact pins and `pnpm install --lockfile-only --offline && pnpm install --frozen-lockfile && pnpm typecheck && pnpm lint` PASS; `pnpm build` PASS (Next 15.5.24); `pnpm check:bundle` PASS (92 routes, max 130/150 kB); `pnpm check:fonts` PASS on retry (55.2/60 kB; initial 240s tool timeout after static-page generation). A production `next start -p 3227` probe of `/_next/image?url=%2Fsw.js&w=64&q=75` with `Host: ops.localhost:3227` returned image optimizer HTTP 400 `The requested resource isn't a valid image`, confirming endpoint reachability even on ops host, not AVIF exploitability. `pnpm audit --prod`: 5 remaining (2 high, 3 moderate): GHSA-6g55-p6wh-862q, GHSA-r28c-9q8g-f849, GHSA-qx2v-qp2m-jg93, GHSA-fxqj-rqcc-2cmp are on Next's pinned PostCSS 8.4.31, not the direct 8.5.26; no route accepting user-supplied CSS/source maps was identified, so build-tool reachability requires monitoring. GHSA-67mh-4wv8-2f99 is esbuild via optional better-auth/drizzle-kit dev tooling, not a shipped server feature. Next AVIF/Windows and sharp findings disappeared. No advisories suppressed. Migration: none. Commit SHA: `041d123`. Deviation: pnpm's normal lock resolution also refreshed compatible transitives; no unrelated direct major update.
- [x] **A2 / F03: Legacy consent attestation.** Explicit checkbox text matches the operator rule; Zod rejects an absent attestation before context/service; the service independently requires a signed-in actor. Existing `consents.evidence` now stores `import_operator_attestation`, tenant-bound import hash, UTC attestation timestamp, operator user ID and optional reference; `witnessed_by_user_id`, `granted_at` and `audit_log.actor_id` are populated. Preview is read-only and displays the no-consent-until-commit instruction; non-import creation still calls the same `createMember` wrapper. Evidence: disposable localhost:55492 DB `pnpm db:reset -- --i-understand` applied 102/102, `pnpm db:deploy` passed; `pnpm exec vitest run tests/member-import.test.ts tests/tier1/consent-schema.test.ts tests/tier1/isolation.test.ts --maxWorkers=1` PASS 3 files / 52 tests; `pnpm typecheck && pnpm lint` PASS. Tests assert adult and minor/guardian, missing attestation, actor/tenant/time/hash/note, cross-tenant isolation, preview no writes and retry no extra consent. Migration: none. Commit SHA: `c4c2176`. Deviation: operator attests to academy-retained external evidence; the app does not independently verify it.
- [x] **A3 / F08: Bound and validate import input.** Server checks UTF-8 byte ceiling (2 MB), 500 source data rows including blank records, replacement/NUL characters, unterminated/misplaced quotes, date ambiguity and actual calendar dates. Export escapes formula prefixes if any CSV error field becomes user-controlled. Evidence: same 3-file/52-test command plus `pnpm typecheck && pnpm lint` passed; 500-row import executed successfully, 501-row preview/commit refused, embedded quoted comma/newline and malformed quote tested. Migration: none. Commit SHA: `c4c2176`. Deviation: textual UTF-8 replacement/NUL detection is best-effort for decoded JavaScript strings, not detection of every possible original byte encoding.
- [x] **A4 / F04: Serial, retry-safe commit.** SHA-256 identity binds tenant to BOM/line-ending/NFC-normalized content; row-level lock on the tenant row serializes imports; the append-only tenant audit row records identity and full row outcomes. One `withTenant()` transaction encloses preview-scoped reads, matches, code allocation, `createMemberInTx`, consent and audit; a thrown error rolls everything back. Sequential and concurrent identical retries read the committed audit result; different uploads serialize; supplied codes are reserved before generated ones. Evidence: same 3-file/52-test run plus prior `pnpm exec vitest run tests/member-import.test.ts --maxWorkers=1` PASS 26/26; includes simultaneous identical/different uploads, induced trigger failure after one row with no retained rows/consent/audit followed by successful retry, generated/supplied code collision, partial valid/error audit and 500-row commit. Migration: none. Commit SHA: `c4c2176`. Deviation: invalid rows are returned as errors while valid rows in the same file commit atomically; if a valid-row DB operation fails, no rows from that file commit.
- [x] **A5: Deterministic complete test gate.** `scripts/run-test-shards.ts` uses Vitest's `--shard=i/4`, one process/file at a time, seeds catalogue once, gathers JSON results, compares all discovered files to the union of four shard reports (fails on missing, duplicate or unexpected files), aggregates failures/skips and reports slowest files. `vitest.config.ts` retains `fileParallelism: false`; no migration/concurrency files run in parallel. Evidence on intended PR diff: `pnpm test:sharded` PASS with **314/314 files, 2488 passed, zero failed, one live-R2 skip**, repeated after restoring the Tier1 file; `pnpm exec vitest run tests/tier1/staff-attendance.test.ts --maxWorkers=1` PASS 7/7; `pnpm typecheck && pnpm lint && pnpm check:location-scope` PASS. Slowest files on final run: `platform-code-gates` 13.0s, rendered ops focus 6.6s, member import 3.9s. Migration: none. Commit SHA: `20276f9`, followed by the documented protected-path restoration. Deviation: sharding is serial across four runner processes to protect shared DB cleanup, not concurrent CI jobs. `tests/tier1/staff-attendance.test.ts` still has two time-of-day skips near IST midnight; human follow-up to replace them with controlled clocks, because `tests/tier1/**` is human-owned (`docs/agent-setup.md`, protected-path workflow). For local/CI commands: use a disposable DB, `pnpm db:reset -- --i-understand && pnpm db:deploy` with its isolated URLs, then `pnpm test:sharded`.
- [x] **A6: PR-A final agent gate.** Frozen install, typecheck, lint, complete sharded suite, build, audit classification, migration and tenant/RLS/permission checks, host boundary, parent zero-JS, import/consent/concurrency tests, bundle/fonts/focus and Compose scanners recorded below. Branch pushed and [PR #192](https://github.com/saurabh22suman/aqua/pull/192) opened into protected `main`, no merge. Evidence: local table below; PR `ci` PASS on run `35993671881` (18m34s) at `baa5508`, including sharded tests, offline e2es, platform-form-leak, host-boundary, parent zero-JS and role-bypass. `agent-protected-paths` FAIL on run `35993671953` pending a human-applied `human-approved-merge` label because `lib/services/consent.ts` changed; **not** a green merge gate. Migration: none. Commit SHA: `baa5508` for the tested PR head; checklist-only evidence follow-up pending. Deviation: five classified transitive advisories remain and live R2 is intentionally skipped (PR-B). Pilot verdict remains NO-GO until PR-B and human operational verification.

### PR-A local verification (disposable Postgres on 127.0.0.1:55492 only)

| Command | Result |
|---|---|
| `pnpm install --frozen-lockfile`; `pnpm typecheck`; `pnpm lint` | PASS; up to date, strict TS and ESLint clean. |
| `pnpm db:reset -- --i-understand`; `pnpm db:deploy` | PASS on disposable DB; 102/102 migrations, repeat deploy no-op and queues initialized; no new migrations. |
| `pnpm test:sharded` | PASS on final code after restoring the human-owned Tier1 file: 4 serial shards, 314/314 files observed exactly once, 2,488 tests passed, zero failed, one live-R2 skip. Original Tier1 IST skip guards remain; at this execution time both attendance cases ran (7/7 targeted). |
| `pnpm exec vitest run tests/tier1/permission-matrix.test.ts tests/tier1/rls-policy-shape.test.ts tests/member-import.test.ts tests/tier1/consent-schema.test.ts --maxWorkers=1` | PASS, 4 files / 58 tests. Earlier import/consent/isolation run: 3 files / 52 passed. |
| `pnpm build`; `pnpm check:bundle`; `pnpm check:fonts`; `pnpm check:focus-contrast` | PASS, Next 15.5.24, 92 routes, largest 130/150 kB, fonts 55.2/60 kB, ring contrast 5.09:1 and 4.43:1. |
| `pnpm check:migrations`; `pnpm check:tenant-conventions`; `pnpm check:location-scope`; `pnpm check:ops-actions`; `pnpm check:platform-leads`; `pnpm check:scripts-exist`; `pnpm check:runbook-sync` | PASS, 102 migration filenames, 34 new-migration conventions and remaining scanners. |
| `pnpm check:compose-secrets`; `pnpm check:dokploy-compose`; `pnpm check:deploy-workflows`; `docker compose -f docker-compose.dokploy.yml config --quiet`; `docker compose -f docker-compose.prod.yml config --quiet` | PASS with dummy Compose interpolation values. No deployment/configuration changes. |
| `pnpm e2e:host-boundary`; `pnpm e2e:parent-link-zero-js` | PASS, 18 host probes and valid/invalid parent links each with zero scripts. Startup connection retries in host test were expected. |
| `pnpm audit --prod` | NON-CLEAN: five transitive advisories (2 high PostCSS source-map read, 2 moderate PostCSS source-map/XSS, 1 moderate dev-tool esbuild). Next AVIF/Windows and sharp advisories resolved. No CSS upload/source-map parsing request path identified; no suppression configured. Reviewed individually under A1; re-evaluate when updating Next. |

**Remaining advisory disposition (not suppressed):**

| Advisory | Locked chain, applicability and follow-up |
|---|---|
| GHSA-6g55-p6wh-862q (high) | `next@15.5.24 > postcss@8.4.31`; attacker-controlled CSS sourceMappingURL file read requires a CSS-processing input. No production route processes uploaded CSS; Next build processes trusted repository CSS. Monitor Next's transitive fix. |
| GHSA-r28c-9q8g-f849 (high) | Same Next/PostCSS 8.4.31 chain; path traversal in previous source map auto-loading. No user CSS entry point found. Monitor Next's fix, reassess on any dynamic CSS ingestion. |
| GHSA-qx2v-qp2m-jg93 (moderate) | Same Next/PostCSS 8.4.31 chain; stringify `</style>` XSS needs untrusted CSS inserted into an HTML style context, not found on request path. Monitor upstream fix. |
| GHSA-fxqj-rqcc-2cmp (moderate) | Same Next/PostCSS 8.4.31 chain; source-map disclosure when `from` is unset. No user CSS processor on request path found. Monitor upstream fix. |
| GHSA-67mh-4wv8-2f99 (moderate) | `better-auth > drizzle-kit > @esbuild-kit/... > esbuild@0.18.20`; advisory affects the dev server, not the compiled Linux Next request path. `drizzle-kit` is a build/development tool; revisit when upgrading its optional dependency chain. |

The source audit's Pilot NO-GO does **not** automatically become a pilot approval from PR-A: PR-B offsite backup/restore and operational checks remain open, plus human review of deployed role workflows. No release gate changed.

## PR-B — Operational Readiness (not started)

- [ ] **B1 / F02:** Offsite R2 backup and restore proof, enabled worker jobs' configuration and backup schedule. Evidence: pending; out of PR-A.
- [ ] **B2 / F05–F06:** Dokploy/Production deployment and runbook parity, immutable image verification and human/VPS checks. Evidence: pending; out of PR-A.
- [ ] **B3:** Human pilot/Production operational release checks and role-specific runtime walkthrough. Evidence: pending; out of PR-A.

## Session log

| Date | Branch | Work / evidence | Status |
|---|---|---|---|
| 2026-09-24 | `fix/pilot-safety` | `git status --short` showed only the untracked audit report; `git fetch --prune origin` left `origin/main` at `c198627`; SHA-256 `f88d8d64…01e8e` before and after `git switch -c fix/pilot-safety origin/main`. | Baseline preserved; initial documentation commit pending. |
| 2026-09-24 | `fix/pilot-safety` | First commit `5251dd3` contains only audit and checklist; report SHA-256 `f88d8d64…01e8e` unchanged after commit. | A0 verified. |
| 2026-09-24 | `fix/pilot-safety` | Next/ESLint config 15.5.24; frozen install, typecheck, lint, build, bundle and font scanner passed; image endpoint returned HTTP 400 from optimizer on ops host; audit reduced from nine to five advisories. | A1 verified; commit pending. |
| 2026-09-24 | `fix/pilot-safety` | Disposable localhost:55492 Postgres applied 102 migrations, `db:deploy` passed. Import/consent/isolation tests 52/52, typecheck and lint passed; no import schema changes. | A2–A4 verified; import commit pending. |
| 2026-09-24 | `fix/pilot-safety` | `c4c2176` committed import and checklist evidence. `pnpm test:sharded` initially found two source-scan failures and one test-fixture phone collision; corrected location scoping and unique phone fixture without removing or relaxing tests. Full rerun: 314/314 files, 2488 passed, 0 failed, 1 live-R2 skip. | A2–A5 locally verified; CI commit pending. |
| 2026-09-24 | `fix/pilot-safety` | `20276f9` added CI serial sharding; its Tier1 clock edit was subsequently reverted under the human-owned tests rule. Frozen install, typecheck, lint, build, bundle/fonts/focus, migrations/scanners, Compose checks, 58 targeted tests, host boundary and parent zero-JS passed; `pnpm audit --prod` reports five classified transitive advisories. | A6 local gate passed; PR and PR CI pending. |
| 2026-09-24 | `fix/pilot-safety` | Protected-path review found `tests/tier1/**` is human-owned; restored the exact original staff-attendance fixture (`git diff origin/main -- tests/tier1/staff-attendance.test.ts` empty). Targeted 7/7 passed. Full sharded rerun: 314/314 files, 2488 passed, 0 failed, 1 live-R2 skip. | A5 gate green on intended final code; human IST test follow-up noted. |
| 2026-09-24 | `fix/pilot-safety` | PR [#192](https://github.com/saurabh22suman/aqua/pull/192) opened into `main` at `baa5508`; GitHub PR `ci` run `35993671881` passed in 18m34s, including all e2es. `agent-protected-paths` run `35993671953` failed because the human-only label is absent. | Agent PR-A gate verified; human review/label required before merge; PR-B not started. |
